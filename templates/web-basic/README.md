# {{PROJECT_NAME}}

Starter project created from `templates/web-basic`.

## Scripts

- `npm run dev` – start the local dev server (lite-server on port 5173)
- `npm run build` – placeholder until a real build is required

Open `index.html` to verify markup, `styles.css` for styling, and `app.js` for entry logic.
