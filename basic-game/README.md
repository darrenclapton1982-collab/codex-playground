# Orb Collector

A tiny self-contained browser game used to test the GitHub push workflow.

## How to run locally

Open `index.html` in a browser.

## Next step for deployment

Connect this repository to Netlify or Vercel and set the publish directory to `basic-game` if you want a live hosted URL.
